# Nightly 2026-09-03 — the first nightly on the merged sheet; the port gate was naming r9 under r10

**Published** `20260903-1130`, live-verified: the live entry reports
`__BUILD = "20260903-1130"`. Exit 0. Gate PASS on baselines · 2,432 events, 90.8% explicit
against a target of 82.6% · climate `intensity0=350.0, floor=40.0`, decline on S, bonus on
`C['C5','C4']` · counts, coverage and tables all clean · 669 authored strings, infrastructure 11%.

**`registry drift · none · r10-2026-09-03`.** The version string is unchanged from the build
this sheet already carried, and the shape is unchanged with it — 11 axes, 61 positions, 59
templates, a criterion for every position. **No plate needs room tonight.**

The r9 → r10 move happened before this run, in yesterday's branch work, not in the pull: the
built `docs/` at `HEAD~2` carried `r9-2026-09-02` and the merge commit brought `r10-2026-09-03`
in. Tonight's pull found the parent still on r10 and drew against it.

## The merge landed, so P0–P5 is live for the first time

The 09-03 morning handoff recorded that Pages serves `main:/docs` while every phase since P0
had changed `web/` on `claude/ai-futures-forecaster-review-t7fvqb` alone — the live sheet was
still the vignette layer a day after P5. `origin/main` now carries that branch, and this
nightly rebuilt `docs/` from the merged `web/` and verified the stamp live. The chronicle, the
provenance marks and the branches plate are on the served sheet.

## A gate label that outlived the thing it named

`build/port_gate.mjs:60` printed the mode as a frozen revision:

```js
const mode = hasDynamics(E) ? 'r9 dynamics' : 'r8 arithmetic';
```

`hasDynamics(E)` is a two-state question — does the emission carry a dynamics block, or must the
port fall back to the r8 arithmetic — and the true branch was named after the revision that
introduced the block rather than after the emission in front of it. Under r10 the gate reported
`port gate · r9 dynamics` on a line where every other build line said `r10-2026-09-03`. Nothing
was mismeasured: 30,000 values compared, 0 divergences, the four crossings agreeing exactly. The
defect was in the reading, and it is the fourth species of the window-label family this project
keeps finding — a key that stays stable while the quantity under it changes.

The mode now names the emission it actually read, from `network.json`, and the fallback says it
is a fallback:

```
port gate · r10-2026-09-03 dynamics · mainline + 25 exemplars · 30000 values compared
```

Re-run against tonight's emission: PASS, 0 divergences, unchanged.

## The sanity findings the readout gate reports and refuses nothing for

18 findings over 13 paths and 374 composed years, all of them properties of the parent's tracks
rather than of the drawing: the likeliest path's capability cap frozen at 6.0 from 2043, the same
freeze on exemplars 1 and 2, exemplar 2's approval frozen at 22.8 from 2070, and two ceilings on
exemplar 2 (AI revenue past a quarter of world output in 2038, employment past 30% below 2026 in
2031). `check_caps` found 0 faults over the 32 composed years past a cap, so nothing is lettered
as a reading where it is a cap. Provenance clean: 0 of 3,728 composed groups without `src`, 0 of
12,312 lines without a source and a kind. Language: 0 of 374 headlines carrying a fault.
Repetition: 0 findings.
