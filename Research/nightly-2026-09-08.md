# Nightly 2026-09-08 — published; three axes have never moved, and two of them have no rule

**Published `20260908-1130`.** Exit 0. Gate PASS on baselines · 2,524 events, 90.7% explicit
against a target of 82.3% · climate `intensity0=350.0, floor=40.0`, decline on S, bonus on
`C['C5','C4']` · counts, coverage and tables clean · 669 authored strings, infrastructure 11% ·
port gate `r10-2026-09-03 dynamics`, mainline plus 25 exemplars, 30,000 values compared, 0
divergences, the four crossings agreeing at `{3: 2033.25, 4: 2034, 5: 2038, 6: 2042.5}` · readout
gate STRICT 0 faults over 13 paths and 374 composed years.

The stamp was not trusted on its own. Every served file was hashed against the local build at the
`?v=` the served entry carries — `index.html` identical, the entry `app.js` 101,619 bytes, and all
eight modules it imports: `draft.js` 31,704, `engine.js` 18,046, `figures.js` 24,301,
`instruments.js` 22,116, `ledger.js` 50,312, `narrative.js` 108,019, `record.js` 31,981,
`sections.js` 96,323. Nine SHA-256 matches. Served `data/meta.json` is identical to the local copy
and carries tonight's corpus: 2,524 events, 1,026 entities, 174 needing review, generated
`20260908-1049`.

Registry `r10-2026-09-03`, unchanged — 11 axes, 61 positions, 59 templates. **No plate needs room.**

## Three axes have never moved, and the reason is that nothing points at them

The sheet draws eleven axis control rows, each with a `LIKELIEST <position> <n>%` reading and a
percentage against every position, taken from the emitted marginals. Across the whole build
history — 35 builds since 2026-08-11 — three of those eleven have taken effectively one value:

```
axis  first compared  nights  nights moved   total movement   distinct emitted vectors
T     2026-08-11        34        12            60.99 pp              13
K     2026-08-17        25         0             0.00 pp               1
A     2026-08-11        34        21            59.21 pp              24
C     2026-08-11        34        20            46.76 pp              21
R     2026-08-17        25         0             0.00 pp               1
D     2026-08-11        34        22            32.51 pp              22
S     2026-08-11        34        19            50.96 pp              21
P     2026-08-11        34        11            60.30 pp              11
E     2026-08-11        34        24            59.41 pp              26
L     2026-08-20        20         1             4.76 pp               2
G     2026-08-21        19        12             0.64 pp              13
```

**K (Takeoff shape) and R (Regulatory architecture) have taken exactly one distinct value in every
build in which they have existed.** L (Laboratory conduct) has taken two: its seed, and the value
it was given on the night it was born, 2026-08-20. Since that night it has been flat for 19
consecutive builds. K and R have been flat for 25.

The cause is upstream and it is exact. Of the 31 rules in the parent's `EVIDENCE_RULES`, the count
that nudge any position on these three axes is zero:

```
axis   rules nudging it   nudges landed (of 192 logged applications)   total nudged
T             9                     37                                   6.829 pp
K             0                      0                                   0.000 pp
A             6                     97                                   8.443 pp
C            23                     80                                  12.271 pp
R             0                      0                                   0.000 pp
D             7                     14                                   3.470 pp
S             7                     61                                   6.032 pp
P             3                      4                                   0.726 pp
E             5                    110                                   4.493 pp
L             0                      0                                   0.000 pp
G            15                      3                                   0.900 pp
```

K, R and L are not axes the evidence has failed to move. They are axes the evidence cannot reach.
No rule in the library names a position on any of them, so no arriving event can ever change their
reading, and the number the sheet prints beside each is a seeded constant drawn in the same ink,
the same size and the same phrasing as C's, which 23 rules update nightly.

This is the defect class this project keeps finding — a surface whose meaning depends on context it
does not show — and it is the one CLAUDE.md already names: *"An axis nothing can move renders a
frozen prior as a live reading."* That warning is attached to **G**, on the evidence that a generous
benefit sweep matched 17 of 2,180 events. **The warning is pointed at the wrong axis.** G carries 15
rules, the second largest set in the library, and it has moved on 12 of its 19 nights and taken 13
distinct values. G is starved — 15 rules earning only 3 applications, four of them
(`ev-clinical-readout`, `ev-clinical-shortfall`, `ev-benefit-delivery`, `ev-benefit-unmeasured`)
never having fired at all — which is a corpus problem, and the corpus problem is correctly
described. K, R and L are a different defect: not starved, unwired.

R is the sharpest case. Regulatory architecture is the axis whose subject the feed carries most of;
the corpus objection that applies to G does not apply to it at all. It has still never moved.

Two secondary readings from the same sweep. Thirteen of the 31 rules have never fired in 192
applications. And the conditional table does declare parents for K (L, T) and R (A, D, E, K, L, P,
S, T) — 23 and 54 tilt entries — so these axes are not roots; L alone is. Yet on 2026-09-03→09-04,
a night T's marginal moved and K's parents therefore moved, K's emitted `K1` stayed at
`0.514764` to the sixth decimal. A recomputation of any parent-sensitive form moves in that
decimal; a reconstruction of the tilt here moved from 0.556438 to 0.556418 on the same input
change. Whatever the emission is doing with these three, it is not recomputing them against their
parents. The mechanism is the Atlas's to diagnose; this note records the observation and its size.

**Held, not applied.** Priors and rules live in the parent, and this task may not write to it.
The recommendation for the next Atlas round: either write evidence rules for K, R and L, or letter
them on the sheet as what they are. Until one of those happens, three of the eleven control rows
are drawing a constant as tonight's reading.

## The explicit-date floor, fourth night below it

```
2026-09-04   n=2456  explicit=2227  pct=90.6759  → rounds to 90.7   PASS
2026-09-05   n=2474  explicit=2243  pct=90.6629  → rounds to 90.7   PASS
2026-09-06   n=2484  explicit=2251  pct=90.6200  → rounds to 90.6   FAIL
2026-09-07   n=2501  explicit=2268  pct=90.6837  → rounds to 90.7   PASS
2026-09-08   n=2524  explicit=2289  pct=90.6894  → rounds to 90.7   PASS
```

The true share is **90.6894%**, against an operative floor of 90.7 (stored baseline 91.7, flat 1.0
tolerance). It passes because `check_events` rounds to one decimal before `audit_all.py:270`
compares. That is the fourth consecutive night the corpus has sat below its own floor with the gate
printing PASS.

**The runway is two undated arrivals**, which is one better than last night:

```
+0 undated  90.6894 → 90.7  PASS
+1 undated  90.6535 → 90.7  PASS
+2 undated  90.6176 → 90.6  REFUSE
```

For a batch of *k* arrivals the line holds at `ceil(0.9065k − 0.8435)` dated — 9 of 10, 18 of 20,
27 of 30. Tonight 23 arrived and 21 carried a date, 91.3%: `2026-09-08-0809-ai-developments.md`
(16 events, 14 dated) and `2026-09-07-2205-ai-developments.md` (7 events, 7 dated). The two that
fell back to the ingest date are Jakub Pachocki's weekend post of September 5–6 and the Department
of Defense's request for authority to redirect $600 million. Incoming batches have now run 79%,
80%, 80%, 100%, 100%, 91.3%. The baseline is still the day-one pair, `recorded 2026-07-31` against
`n: 1825`, enforced tonight against a corpus 38% larger.

The Atlas owns this. Do not set `SKIP_AUDIT`.

## Both change printers silent for a second night, and one of them is blind twice

No `DRIFT` line and no `RATCHET` line, for the same reason as 2026-09-07: the Atlas's own nightly
ran first — staged at 10:49, this gate at 11:30 — and wrote `last_run` and the tightened baselines
before this gate read them. `baselines.json` now holds `last_run.date: 2026-09-08` with tonight's
exact numbers, so every comparison found `now == was` and printed nothing. Two things moved
unreported: `forecast.grounded_direct` 1664 → 1669 and `review.pct` 6.957 → 6.894.

There is a second blindness underneath the first, and it survives fixing `last_run`. The DRIFT
printer compares `events.explicit_pct`, which is the **rounded** value. Tonight's move, 90.6837 →
90.6894, is real and is the quantity the gate's own runway depends on, and it would print nothing
even with a correct `last_run`, because both nights round to 90.7. The printer that exists to catch
erosion inside the tolerance cannot see the digits the erosion happens in.

## `review.pct` ratcheted on a night nothing was reviewed

`review.pct` is `needs_review / len(events)`. Reconstructing the numerator from the recorded rates:

```
2026-09-06   173 / 2484 = 6.965%
2026-09-07   174 / 2501 = 6.957%   backlog ROSE by one; the rate printed an improvement
2026-09-08   174 / 2524 = 6.894%   backlog unchanged; the rate printed an improvement
```

The backlog has not fallen on either night. It rose on 09-07 and stood still on 09-08, and both
nights the gate recorded an improvement and ratcheted the baseline tighter on it — to 6.957, then
to 6.894 — because the denominator is a corpus that only grows. The rate was introduced
deliberately, and the comment at `audit_all.py:248` gives the reason: a rising absolute count can
mean nothing worse is happening. That is true. But the absolute count is now checked nowhere. The
`review.n` comparison at line 297 sits behind an `elif` on `"pct" in b["review"]`, and `pct` has
been present in the baseline ever since it was recorded, so that branch is unreachable. The number
of documents awaiting review can rise every night without any check reading it.

At tonight's corpus the 0.5-point band allows 187 documents before refusal, against 174 held.

## Zero-margin baselines, unchanged from last night

`forecast.grounded_direct` is compared with no tolerance at all — `r < b` refuses — and its
baseline is now tonight's 1669. `witness_epoch.matched` stands at 20 against a baseline of 20,
`witness_epoch.disagreements` at 4 against 4, and `statelaw.observed` at 8 against 8 under a rule
that an enumeration may not shrink. One step down on any of them refuses the publish. Only
`events.explicit_pct` and `events.target_pct` carry the 1.0-point band, and `review.pct` a 0.5.

## Tonight's prior movement

Total absolute movement across all eleven axes: **1.2091 pp**. D carries 0.5178 of it (42.8%) and
D with P carries 0.7687 (63.6%). Five axes moved exactly 0.0000 pp — T, K, R, L and G. T's stillness
is three nights old and is the Atlas's own finding for tonight; K, R and L are the standing
condition described above; G moved last night and did not tonight.

```
D  0.5178    P  0.2509    A  0.1141    C  0.1126    S  0.1075    E  0.1062
T  0.0000    K  0.0000    R  0.0000    L  0.0000    G  0.0000
```

Largest single moves: `D2` 0.3579 → 0.3605 (+0.2589 pp), `D1` 0.2373 → 0.2354 (−0.1915 pp),
`P2` 0.2326 → 0.2338 (+0.1254 pp).
