# Nightly 2026-08-30 — the parent's drift panel fabricates four rows; this sheet draws none of them

**Published** `20260830-1130`, live-verified on data the sheet draws: the live
`data/forecast/delta.json` is byte-identical to the local build (28,888 bytes, matching SHA-256)
and returns `date: 2026-08-30`, and the live entry module resolves to `js/app.js?v=20260830-1130`.
Gate PASS · 2,364 events, 90.9% explicit against a target of 82.4% · climate `intensity0=350.0,
floor=40.0` · coverage and counts both clean · 2,897 authored strings, infrastructure 13%.
`registry drift · none · r8-2026-08-20` — the version string is unchanged and truthful tonight,
so **no plate needs room**. The commit is data and version stamps only.

Five nights published quietly between the 08-24 note and this one.

## The parent found a defect in its own drift panel; the same defect had a place to live here

The Atlas's morning report tonight (§3) records `drift30()` in its `web/index.html` fabricating
four of its eleven rows. `((past[k]||{})[pos]||0)` gives an axis that did not exist at the
baseline a baseline of zero, so the subtraction returns the axis's entire present probability and
the panel letters it as movement:

```
K.K1  51.48pp   absent at baseline   panel shows +51.5pp
G.G2  25.89pp   absent at baseline   panel shows +25.9pp
R.R2  24.75pp   absent at baseline   panel shows +24.8pp
L.L4  22.57pp   absent at baseline   panel shows +22.6pp
```

This sheet reads the same `marginals.json`. **The same four axes are absent from its baseline row,
carrying the same four probabilities** — K and R were created at r5 on 08-17, L and G at r7 and r8
on 08-20, and the baseline row is 2026-07-31. Every condition for the defect is present here.

It draws none of them, and the reason is one operator. `app.js:371` reads

```js
const now = (m[p[0]] || 0) * 100, was = lb.days === null ? null : (w30[p[0]] ?? null);
```

`?? null` where the parent has `|| 0`, and the clause below it is `was === null ? '' : …`, so a
position with no baseline gets **no drift clause at all**. Checked all three consumers of the
lookback baseline, since one clean site proves nothing about the others:

| site | reads | absent baseline |
|---|---|---|
| `app.js:371` axis notes | every axis, every position | `?? null` → clause omitted |
| `sections.js:1123` instruments dial | axis T only | `?? null` → `dial()` skips the pale needle |
| `sections.js:355` chart-head dial | axis T only | `?? null`; T exists at the baseline anyway |

Both instrument bodies gate the pale needle on `was !== null` (`instruments.js:46` and `:115`), so
a face with no baseline draws one needle and the standing text says so: "A single needle: this setting holds one reading, so there
is no gap to draw."

The sheet is clean here **by construction**, and the construction is six characters wide. Worth
recording precisely because nothing in the build would have caught it — the coverage gate compares
axes and position keys, both of which agree; the collision audit measures marks, and four
fabricated rows collide with nothing. A drawing can be fully consistent and still letter a number
that no measurement produced.

## The same history file gives the two projects different baselines, and the difference is 6 days

`marginals.json` holds **37 rows over 31 distinct dates** — a registry re-set writes a second row
for the same day. The two lookbacks read that file on the same night and disagree:

```
parent   h[h.length-31]        row index   lands 2026-08-06   24 days   heading says "30-day"
here     newest row ≤ to−30d   by date     lands 2026-07-31   30 days   letters "30 days"
```

This is the window-label defect in a side-by-side demonstration: same file, same night, one
answer picked by row count and one picked by date. The comment at `app.js:347` already states the
rule, and tonight is the first time the project has been able to show what the other branch
returns. The span this sheet letters is the span it measured, which is why the two numbers being
different is a result rather than a discrepancy.

## `registry_drift()` exists here, and the parent has hand-checked it for seven nights

The Atlas's report tonight, §1:

> **Registry drift, checked by hand: none.** … `registry_drift()` **still does not exist in the
> repo**; grepped `build/` and `Research/` again, the only hits are prior reports saying so.
> **Seventh night of hand checks.**

It exists at `build/build_site.py:186` in **this** project, added 2026-08-24 in response to
`D.watch-labor` arriving under an unchanged version string. Confirmed by grep: zero non-report
hits anywhere in `~/AI Atlas`. So the function the parent's operator has been looking for lives
one project downstream, and has been running nightly since the 24th — it printed
`registry drift · none · r8-2026-08-20` tonight, the same conclusion the hand check reached.

Held, not applied. Priors and parent code are decisions, and this note is the case, not the
change. The two checks also read different references — this one diffs the emitted `network.json`
against the last committed copy, the parent's hand check diffs against last night's emitted file
per its 08-29 method note — so they are close but not the same instrument.

## What holds

Position counts per axis unchanged, 11 axes and 61 positions. Sub-axes 8, with `D.watch-labor`
still filed under "Sub-axes — provisional, awaiting approval" as an uncited machine addition.
`climate_params()` extracted cleanly, so the extractor still matches the parent's `worldlines.py`.
The gate's three ratchet tightenings tonight (`events.target_pct` 82.2 → 82.4, `review.pct`
7.437 → 7.403, `forecast.grounded_direct` 1571 → 1578) are the parent's and are recomputed each
run; they land near the frozen 07-31 baseline because the ratchet has climbed back to it, and
none of them is inherited.

The evidence window runs 40 entries over nine dates, 08-22 to 08-30. Tonight's two applications
are both `ev-compute-buildout` at the identical vector `S2 +0.048pp · S3 −0.024pp` — a chip
foundry in Bastrop and datacenter robots that plug in cables. That is the fixed-vector finding in
its fifth instance; it belongs to the parent's rule set, and the parent's own report (§4) opens on
it tonight.
