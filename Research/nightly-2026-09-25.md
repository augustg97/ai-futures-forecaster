# Nightly 2026-09-25 — the gate refused a sixth night

**Nothing published.** `build/nightly.sh` exited 1 at its first step: the AI Atlas corpus gate
refused, so the pull never ran, `docs/` was never stamped, and the build committed nothing. The
working tree was clean at `0415e57` before the run and the live sheet still serves
`__BUILD = "20260919-1130"` (curl). `SKIP_AUDIT` was not set. The Atlas's own nightly owns the
failure; its `Research/nightly-reports/2026-09-25.md` carries the diagnosis. `~/AI Atlas` was
read only.

## The baseline that moved

    GATE: FAIL
     - review.pct moved backwards: 15.262 (baseline ≤ 6.447)

The gate refuses at `value > 6.447 + 0.5`, so **the number it compared against is 6.947**. The
value is 15.262% (440 / 2,883), which fails by **8.315 pp** (09-24: 8.543; 09-23: 8.362).
This is the first night the failure margin shrank: needs-review fell 443 → 440 while events
grew 2,860 → 2,883.

| check | 09-19 (live) | 09-24 | 09-25 | |
|---|---|---|---|---|
| `review.pct` | 6.485 | 15.490 | **15.262** | ERODED — the refusal |
| `review.n` | 178 | 443 | **440** | −3 |
| `events.n` | 2,745 | 2,860 | 2,883 | +23 |
| `forecast.grounded_direct` | 1,826 | 1,994 | 2,021 | +27; not eroded |
| `events.explicit_pct` | 90.9 | 91.0 | 90.9 | 2,622 / 2,883 = 90.947%; gate needs 2,614, margin **+8 events** |
| `events.target_pct` | 83.2 | 83.4 | 83.5 | not eroded |
| witness matched / disagreements | 20 / 4 | 20 / 4 | 20 / 4 | held |
| windows · cards · datekinds · eras · statelaw · `datum.max_drift` | clean | clean | clean | |

The explicit-date event margin is from the Atlas's report, which ran it from the census.
It has gone +10 → +8 in one night; it is the next gate check likely to refuse.

**Same cause as the last five nights.** The Atlas measured the `evaluations/` dangling census at
**261** (260 last night); the other dangling links fell 137 → 133. `frames.py:43 FOLDERS`
names 13 wiki folders against a 14-folder vault; the fix is August's call and is not taken
here. Without that class the night is **6.209%**, inside 6.947 by **0.738 pp** — the margin
widened for the first time (0.548 last night).

## The forecast defects waiting behind the gate

The Atlas reports `weights.json` has advanced six times unpublished: **24 applications,
total magnitude 0.0263664**, including two `ev-benefit-delivery` matches on `medicare*`
(breach stories scored as benefit) and an Oracle force-majeure item scored as build-out.
Whoever opens the gate publishes all of it at once; the Atlas recommends fixing
`ev-benefit-delivery` before or with the `FOLDERS` fix. The first publishing night here should
check G.G1 for that shift.

## The registry did not move, and no plate needs room

`network.json` is `r10-2026-09-03` in both the staged emission
(`~/AI Atlas/Research/staged/forecast/`) and the last build (`docs/data/forecast/`): 11 axes on
each side. No new axis or sub-axis.

## State after the run

- Live: `20260919-1130`, the designed fallback; yesterday's drawing is still served.
- Repo: nothing committed by the build. This note is the only new file.
- No extractor or pull problem. The run never got that far, so exit 2 was never exercised.
