# Nightly 2026-09-07 — published after a two-night hold; the recovery was arrival composition

**Published `20260907-1129`.** Exit 0. The sheet had been serving `20260905-1131` for two nights
after the 2026-09-06 refusal; it now serves tonight's build. Gate PASS on baselines · 2,501 events,
90.7% explicit against a target of 82.4% · climate `intensity0=350.0, floor=40.0`, decline on S,
bonus on `C['C5','C4']` · counts, coverage and tables clean · 669 authored strings,
infrastructure 11% · port gate `r10-2026-09-03 dynamics`, mainline plus 25 exemplars, 30,000
values compared, 0 divergences, the four crossings agreeing at
`{3: 2033.25, 4: 2034, 5: 2038, 6: 2042.5}` · readout gate STRICT 0 faults over 13 paths and 374
composed years.

The stamp was not trusted on its own. Every served file was hashed against the local build at the
`?v=` the served entry carries — the entry `app.js` 101,619 bytes and all eight modules it imports,
`draft.js` 31,704, `engine.js` 18,046, `figures.js` 24,301, `instruments.js` 22,116, `ledger.js`
50,312, `narrative.js` 108,019, `record.js` 31,981, `sections.js` 96,323. Nine SHA-256 matches.
Served `data/meta.json` is identical to the local copy and carries tonight's corpus: 2,501 events,
1,025 entities, 174 needing review, generated `20260907-1048`.

## What lifted the gate back over its floor

```
2026-09-04   n=2456  explicit=2227  pct=90.6759  → rounds to 90.7   PASS
2026-09-05   n=2474  explicit=2243  pct=90.6629  → rounds to 90.7   PASS
2026-09-06   n=2484  explicit=2251  pct=90.6200  → rounds to 90.6   FAIL
2026-09-07   n=2501  explicit=2268  pct=90.6837  → rounds to 90.7   PASS
```

Seventeen events arrived since the refusal and **all seventeen carried an explicit date** — the two
batches `2026-09-06-2209-ai-developments.md` (10 events, 10 dated) and
`2026-09-07-0809-ai-developments.md` (7 events, 7 dated), both at 100%, against 80.0%, 80.0% and
79% in the batches that produced the slide. The 09-06 note put the requirement at eight consecutive
fully-dated events; seventeen arrived and the corpus rose 0.0637 pp.

**Nothing was fixed.** The corpus recovered because two batches happened to be well dated. Counted
across the whole span since the last published build, 27 events arrived and 25 carried a date —
92.6%. The two that did not both fell back to the ingest date: an update on the G20 innovation
ministerial in Chapel Hill, and Anthropic's agreements for at least 14.8 GW of compute.

## The gate is still inside its rounding blind band

Counted from the staged trunk rather than read off the summary line, the corpus stands at
**90.683727%**. The operative floor is 90.7 — stored baseline 91.7, flat 1.0 tolerance,
`audit_all.py:270` — so the corpus is **0.016 points below the floor it passed**, exactly as on
2026-09-04 and 2026-09-05. What stands between this project and a refused publish is still
`round(100 * expl / n, 1)` in `check_events` at `audit_all.py:123`, which compares a value already
rounded to one place.

The runway is again one event: a single undated arrival gives `2268/2502 = 90.6475` → 90.6, and the
gate refuses. Stated for a batch rather than a single event, an arriving batch of *k* needs
`ceil(0.9065k − 0.8435)` dated to hold the line — **9 of 10, or 15 of 17**. Incoming batches have
run 79%, 80%, 80%, 100%, 100%. An ordinary ten-event batch at 80% refuses.

**The baseline still has never been re-taken.** `recorded: 2026-07-31`, against `n: 1825`. It was
enforced tonight against 2,501 events, a corpus **37% larger** than the one the number was measured
on. That is the Atlas's to decide and its own nightly owns it; `SKIP_AUDIT` was not set and must
not be.

## Both of the Atlas's change printers were silent, and tonight they hid good news

The 09-04 note recorded that the day-over-day drift printer goes silent when the Atlas's own
nightly runs first and overwrites `last_run` before this project's gate reads it. Tonight the same
thing happened to the ratchet printer as well.

`baselines.json` now holds `last_run.date = 2026-09-07` with `explicit_pct 90.7`, and its stored
baselines carry `forecast.grounded_direct 1664` and `review.pct 6.957` — tonight's exact values.
So three things moved tonight and this project's run printed none of them:

- `events.explicit_pct` 90.6 → 90.7, the recovery this whole note is about;
- `forecast.grounded_direct` 1653 → 1664;
- `review.pct` 6.965 → 6.957.

The second and third are the two figures the 09-06 refusal discarded, because `run()` returns on
`failures` before it reaches the ratchet. They were re-earned tonight and then some — 1645 → 1653
→ 1664. No `DRIFT` line and no `RATCHET` line appeared in the output this project saw, because by
the time this gate ran, `now == was` on every path.

This is the same defect the 09-04 note recorded — `last_run` is named for the last run and read as
though it meant yesterday — reaching a second instrument. Two new things follow from tonight. The
ratchet printer is blind for exactly the same reason as the drift printer, so **neither of the
Atlas's two change reporters can fire for a second reader of an already-recorded snapshot.** And
the blindness is symmetric: it hid the erosion on 2026-09-04 and it hid the recovery on 2026-09-07,
so it cannot be characterised as a gate that only conceals bad news. The fix belongs in the Atlas —
key the comparison to a date rather than to a run — and is noted here for whoever holds that
decision.

## The explicit-date floor is not the only floor at zero margin

The ratchet's design is that an improved metric becomes the new floor, "so the tolerance band is
spent once and never handed back". The consequence is that after any improving night, the
ratcheted metrics sit at exactly zero margin. `forecast.grounded_direct` is compared with no
tolerance at all — `r < b` refuses — and its baseline is now tonight's 1664. `witness_epoch.matched`
stands at 20 against a baseline of 20, `witness_epoch.disagreements` at 4 against 4, and
`statelaw.observed` at 8 against 8 under a rule that an enumeration may not shrink. Only
`events.explicit_pct` and `events.target_pct` carry the 1.0-point band, and `review.pct` a 0.5.
The explicit-date share is the floor being watched; it is not the only one a single step would
cross.

## Registry: no change, no plate needed

The staged emission is `network.json` at **`r10-2026-09-03`** — the same version string the build
this sheet already carried, with the same shape under it: **11 axes, 61 positions, 59 templates**,
a criterion for every position, onset rules on 29, `LONGFORM` lacking none. `registry drift · none`.
No new axis or sub-axis, so nothing needs room on the sheet.

## The sanity findings the readout gate reports and refuses nothing for

19 findings over 13 paths and 374 composed years, all properties of the parent's tracks rather than
of the drawing: the likeliest path's capability cap frozen at 6.0 from 2043 and its approval frozen
at 32.5 from 2071, the same capability freeze on exemplars 1 and 2, and two ceilings on exemplar 2
— AI revenue past a quarter of world output in 2038, employment past 30% below 2026 in 2031.
`check_caps` found 0 faults over the 62 composed years past a cap. Provenance clean: 0 of 3,719
composed groups without `src`, 0 of 12,184 lines without a source and a kind. Language: 0 of 374
headlines carrying a fault. Repetition: 0 findings.
