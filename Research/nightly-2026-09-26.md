# Nightly 2026-09-26 — the gate refused a seventh night

**Nothing published.** `build/nightly.sh` exited 1 at its first step: the AI Atlas corpus gate
refused, so the pull never ran, `docs/` was never stamped, and the build committed nothing. The
working tree was clean at `867483b` before the run and the live sheet still serves
`__BUILD = "20260919-1130"` (curl). `SKIP_AUDIT` was not set. The Atlas's own nightly owns the
failure; its `Research/nightly-reports/2026-09-26.md` carries the diagnosis. `~/AI Atlas` was
read only.

## The baseline that moved

    GATE: FAIL
     - review.pct moved backwards: 15.172 (baseline ≤ 6.447)

The gate refuses at `value > 6.447 + 0.5`, so **the number it compared against is 6.947**. The
value is 15.172% (440 / 2,900), which fails by **8.225 pp** (09-25: 8.315; 09-24: 8.543).
Needs-review held at 440 while events grew 2,883 → 2,900; the shrink is all denominator.

| check | 09-19 (live) | 09-25 | 09-26 | |
|---|---|---|---|---|
| `review.pct` | 6.485 | 15.262 | **15.172** | ERODED — the refusal |
| `review.n` | 178 | 440 | **440** | ±0; census identical item for item (Atlas) |
| `events.n` | 2,745 | 2,883 | 2,900 | +17 |
| `forecast.grounded_direct` | 1,826 | 2,021 | 2,046 | +25; not eroded |
| `events.explicit_pct` | 90.9 | 90.9 | 91.0 | 2,639 / 2,900; gate needs 2,629, margin **+10 events** (was +8) |
| `events.target_pct` | 83.2 | 83.5 | 83.6 | not eroded |
| witness matched / disagreements | 20 / 4 | 20 / 4 | 20 / 4 | held |
| windows · cards · datekinds · eras · statelaw · `datum.max_drift` | clean | clean | clean | |

The explicit-date margin is from the Atlas's report, which ran it from the census; all 17 new
events were explicitly dated, so the fallback count held at 261.

**Same cause as the last six nights.** The `evaluations/` dangling class is **261** (unchanged);
other classes 133 + 46. `frames.py:43 FOLDERS` names 13 wiki folders against a 14-folder
vault; the fix is August's call and is not taken here. Without that class the night is
**6.172%**, inside 6.947 by **0.775 pp** (was 0.738).

## The forecast defects waiting behind the gate

The Atlas reports `weights.json` has advanced seven times unpublished: **29 applications**
(5 tonight). Still banked: the two `ev-benefit-delivery` `medicare*` misclassifications
(`axes.py:3104`, 0.0055611), and now two `ev-compute-buildout` sign issues — Oracle's
force-majeure item (09-25) and tonight's Crusoe turbine-order cancellation (S.S2 +0.00063), both
build-out *stress* scored as capacity. The first publishing night here should check G.G1 and
S.S2 for those shifts.

## The registry did not move, and no plate needs room

`network.json` is `r10-2026-09-03` in both the staged emission and `docs/data/forecast/`:
11 axes on each side. No new axis or sub-axis.

## State after the run

- Live: `20260919-1130`, the designed fallback; yesterday's drawing is still served.
- Repo: nothing committed by the build. This note is the only new file.
- No extractor or pull problem; exit 2 was never exercised.
