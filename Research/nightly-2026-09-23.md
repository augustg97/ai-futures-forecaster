# Nightly 2026-09-23 — the gate refused a fourth night; the refusal barely grew

**Nothing published.** `build/nightly.sh` exited 1 at its first step: the AI Atlas corpus gate
refused, so the pull never ran, `docs/` was never stamped, and the build committed nothing. The
working tree was clean at `4d8fe8e` before the run and the live sheet still serves
`__BUILD = "20260919-1130"` (curl). `SKIP_AUDIT` was not set. The Atlas's own nightly owns the
failure; its `Research/nightly-reports/2026-09-23.md` carries the diagnosis. `~/AI Atlas` was
read only.

## The baseline that moved

    GATE: FAIL
     - review.pct moved backwards: 15.309 (baseline ≤ 6.447)

The gate refuses at `value > 6.447 + 0.5`, so **the number it compared against is 6.947**. At
15.309 the night failed by **8.362 pp** (09-22: 8.342; 09-21: 2.033; 09-20: 0.873).

| check | 09-19 (live) | 09-22 | 09-23 | |
|---|---|---|---|---|
| `review.pct` | 6.485 | 15.289 | **15.309** | ERODED — the refusal |
| `review.n` | 178 | 429 | **434** | +5 |
| `events.n` | 2,745 | 2,806 | 2,835 | +29 |
| `forecast.grounded_direct` | 1,826 | 1,954 | 1,971 | +17; not eroded |
| `events.explicit_pct` | 90.9 | 91.0 | 90.9 | 2,578 / 2,835; gate needs 2,570, margin **+8 events** |
| `events.target_pct` | 83.2 | 83.2 | 83.1 | printed ERODED against 09-19's `last_run`; 26 events inside the gate |
| witness matched / disagreements | 20 / 4 | 20 / 4 | 20 / 4 | held |
| windows · cards · datekinds · eras · statelaw · `datum.max_drift` | clean | clean | clean | |

Figures in the `explicit_pct` and `target_pct` rows are from the Atlas's report, which ran them
from the census.

**Same cause as the last three nights.** The Atlas measured the `evaluations/` dangling census
at **254** (250 last night). `frames.py:43 FOLDERS` names 13 wiki folders against a 14-folder
vault; the fix is August's call and is not taken here. The Atlas puts the night at
**6.349%** without that class, which is inside 6.947 and would have published. Growth has nearly
stopped (+4 against +179 last night), but 254 is well over the ceiling and waiting will not
clear it. The 876-link ceiling measured on 09-22 still stands.

The 434 count is the census line in `Research/staged/needs-review.md`. The list under it is
still capped at 400 by `emit.py:476`, so the count, not the list, is the source.

## The registry did not move, and no plate needs room

`network.json` is `r10-2026-09-03` in both the staged emission and the last build: 11 axes,
61 positions on each side. No new axis or sub-axis.

## State after the run

- Live: `20260919-1130`, the designed fallback; yesterday's drawing is still served.
- Repo: nothing committed by the build. This note is the only new file.
- No extractor or pull problem. The run never got that far, so exit 2 was never exercised.
