# Nightly 2026-09-21 — the gate refused a second night; the sheet keeps serving 09-19

**Nothing published.** `build/nightly.sh` exited 1 at its first step: the AI Atlas corpus gate
refused, so the pull never ran, `docs/` was never stamped, and the build committed nothing. The
working tree was clean at `15e3df7` and the live sheet still serves `20260919-1130`, verified by
curl against `docs/index.html` with a cache-busting query. `SKIP_AUDIT` was not set; the Atlas's
own nightly owns the failure and its `Research/nightly-reports/2026-09-21.md` carries the
diagnosis. `~/AI Atlas` was read only.

## The baseline that moved

    GATE: FAIL
     - review.pct moved backwards: 8.98 (baseline ≤ 6.447)

The same check as last night, further out. The gate tests `value > 6.447 + 0.5`, so the number
it compared against is **6.947**, and at 8.980 the night failed by **2.033 pp** (09-20: 0.873).

| check | 09-19 (live) | 09-20 | 09-21 | |
|---|---|---|---|---|
| `review.pct` | 6.485 | 7.820 | **8.980** | ERODED — the refusal |
| `review.n` | 178 | 216 | **250** | +34 tonight, nothing removed |
| `events.n` | 2,745 | 2,762 | 2,784 | +22 |
| `forecast.grounded_direct` | 1,826 | 1,859 | 1,871 | +12 |
| `events.explicit_pct` / `target_pct` | 90.9 / 83.2 | 90.9 / 83.2 | 90.9 / 83.2 | held |
| witness matched / disagreements | 20 / 4 | 20 / 4 | 20 / 4 | held |
| windows · cards · datekinds · eras · forecast · statelaw · `datum.max_drift` | clean | clean | clean | |

The rate eroded on its numerator again: needs-review +15.7% (216 → 250) against an event trunk
+0.80% (2,762 → 2,784). All 34 new lines are `dangling relationship target 'evaluations/…'`, and
the class is now **71 of the 250**. Without it the night is 179 / 2,784 = **6.430%**, inside the
ceiling by 0.517, and it would have published.

**This refusal cannot clear by waiting.** Last night it read as a one-off migration. Tonight's 34
came from model and system cards the vault rebuilt against its new benchmark pages, so every card
ingested adds to the count. The fix is the Atlas's `frames.py:43 FOLDERS` (and `FEATURE_FOLDERS`,
`DECAY`), and the Atlas report leaves it to August. This surface keeps serving 09-19 until then,
which is the designed behaviour of standing rule 2. No queue task belongs to this project.

## The registry did not move

`network.json` is `r10-2026-09-03`, eleven axes, in the staged emission and in the last build.
Conditionals, stories, impact classes, changelog and every axis and position key are identical;
what differs inside `axes` is the evidence-weighted prior on A, C, E and G. No plate needs room.

## What is being withheld is still the drawing on the sheet

A seventh consecutive night on which every lettered quantity is identical:

| | built (live, 09-19) | staged (withheld, 09-21) |
|---|---|---|
| medoid | `T3 K1 A4 C1 R2 D2 S1 P2 E3 L4 G1` | identical |
| argmax | `T2 K1 A2 C1 R4 D2 S1 P3 E3 L4 G4` | identical |
| crossings | 2033.25 / 2034 / 2038 / 2042.5 | identical |
| ledger events | 15 | identical, same fifteen ids and years |
| onsets · tracks · layers · `engine.json` | | identical |
| agreement | 0.2953 | 0.2950 |
| medoid `p` | 4.211767e-07 | 4.146674e-07 — a **1.546% fall** |
| argmax `p` | 7.277710e-06 | 7.308742e-06 — a **0.426% rise** |

The medoid's probability moved 140 times as far as last night's −0.011%, the argmax's eight times
its +0.051%, and two things under the medoid changed for the first time in the run:

- **One exemplar swapped.** `T4 K1 A3 C1 R6 D1 S3 P5 E3 L6 G2` left the 120 and the same line with
  `A2` entered — the A2 lift below, arriving in the drawn set. The 59 template ids across the
  exemplars are the same set, and A2 is already authored (it is the argmax's position).
- **The ensemble bands moved** at 307 values (268 track, 39 monthly). The largest is the median
  count of running AI copies in 2032, 273,911 → 242,017 (−11.6%); every other band value moved
  under 6.3%.

Every staged template and medoid position is already authored, so `table_coverage.mjs` and the
readout gate have nothing to refuse whenever the parent certifies.

## The delta, measured two ways

Marginals from the staged history, per night, and against what the reader sees:

| axis | 09-19 → 09-20 | 09-20 → 09-21 | live → staged | largest position, live → staged |
|---|---|---|---|---|
| A | 0.1392 | **0.6526** | **0.6925** | `A4` −0.2064 pp (`A2` +0.2110 tonight) |
| C | — | 0.1268 | 0.1268 | `C4` +0.0634 pp |
| E | — | 0.1080 | 0.1080 | `E1` +0.0540 pp |
| G | 0.0436 | — | 0.0436 | `G2` −0.0186 pp |
| D | 0.0225 | 0.0225 | **0.0000** | `D2` +0.0113 then −0.0113, an exact reversal |
| T K R S P L | — | — | — | |
| **total L1** | 0.205 pp | **0.910 pp** | **0.971 pp** | |

Tonight moved four and a half times as far as last night, and the first certified night will
move the sheet by 0.971 pp in one step. The Atlas measured that **three of tonight's five
applications re-apply developments already applied**, carrying 73% of the applied magnitude and
0.653 of the 0.887 pp evidence movement: the Gemini breakout now counted three times under two
rules (09-19 research, 09-21 research, 09-21 incident), and Amodei's *We Must Pace the Frontier*
on 09-12, 09-14 and 09-21. The single Verge report of the breakout is 53% of the night's magnitude
and the one application that writes `A2`. So the largest move this sheet will draw when it next publishes is
mostly the same story counted again. That defect is the Atlas's, reported in its own note.

C and E moved on evidence for the first time in this refusal run (Google's lobbying for state
chatbot-safety bills; Arete's capex-to-cash-flow claim at the AI Infra Summit). K, R and L are
flat again, as every night since the 2026-09-08 measurement that named them unwired.

## Correction to the 09-20 note

Last night's note said fifteen `ev-safety-research` entries were applied that night, and that
OpenAI's misalignment framework appeared three times in one night on a fourth consecutive night.
**Both are wrong.** `delta.json` holds a rolling window of application dates, 09-12 to 09-21
tonight, with sixteen safety-research entries across it. On 09-20 there was **one** application,
Anthropic's alignment assessment of four incidents (`A3` +0.075 pp requested), as the Atlas's
09-20 report says. The framework was applied once on each of 09-17, 09-18 and 09-19 and did not
return on 09-20. The 0.139 pp on A was right; its attribution was not. The note read a window
with no label as a night. The fix is to filter `entries` on `date` before counting a night, as
the tables above do.

## Standing

Two refusals running, one cause, and the margin widening by the ingest. The sheet did the right
thing both nights. The drawing behind the gate is still the one on the sheet in every lettered
quantity, but it has started to move underneath: one exemplar, the bands, and the medoid's
probability moving 140 times as far as last night. When the parent next certifies, the reader gets two nights of
evidence at once, and most of it is a repeat.
