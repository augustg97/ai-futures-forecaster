# Nightly 2026-09-24 — the gate refused a fifth night

**Nothing published.** `build/nightly.sh` exited 1 at its first step: the AI Atlas corpus gate
refused, so the pull never ran, `docs/` was never stamped, and the build committed nothing. The
working tree was clean at `5e02ae8` before the run and the live sheet still serves
`__BUILD = "20260919-1130"` (curl). `SKIP_AUDIT` was not set. The Atlas's own nightly owns the
failure; its `Research/nightly-reports/2026-09-24.md` carries the diagnosis. `~/AI Atlas` was
read only.

## The baseline that moved

    GATE: FAIL
     - review.pct moved backwards: 15.49 (baseline ≤ 6.447)

The gate refuses at `value > 6.447 + 0.5`, so **the number it compared against is 6.947**. The
unrounded value is 15.490% (443 / 2,860), which fails by **8.543 pp** (09-23: 8.362; 09-22:
8.342; 09-21: 2.033; 09-20: 0.873).

| check | 09-19 (live) | 09-23 | 09-24 | |
|---|---|---|---|---|
| `review.pct` | 6.485 | 15.309 | **15.490** | ERODED — the refusal |
| `review.n` | 178 | 434 | **443** | +9 |
| `events.n` | 2,745 | 2,835 | 2,860 | +25 |
| `forecast.grounded_direct` | 1,826 | 1,971 | 1,994 | +23; not eroded |
| `events.explicit_pct` | 90.9 | 90.9 | 91.0 | 2,603 / 2,860; gate needs 2,593, margin **+10 events** |
| `events.target_pct` | 83.2 | 83.1 | 83.4 | 2,384; margin +34 events |
| witness matched / disagreements | 20 / 4 | 20 / 4 | 20 / 4 | held |
| windows · cards · datekinds · eras · statelaw · `datum.max_drift` | clean | clean | clean | |

Event-count margins are from the Atlas's report, which ran them from the census.

**Same cause as the last four nights.** The Atlas measured the `evaluations/` dangling census at
**260** (254 last night). `frames.py:43 FOLDERS` names 13 wiki folders against a 14-folder
vault; the fix is August's call and is not taken here. Without that class the night is
**6.399%**, inside 6.947 by 0.548 pp — a margin that has shrunk three nights running
(0.598 → 0.568 → 0.548 as reported by the Atlas). Fixing `FOLDERS` still clears the gate, but
the other classes are growing (42 → 43 → 46) and are eating the room.

The 443 count is the census, rebuilt by the Atlas from `emit.load_*`; `needs-review.md` is
still a preview capped at 400 by `emit.py:476` ("…and 43 more.").

## A forecast defect the gate did not catch

The Atlas report flags that `ev-benefit-delivery` matched an OpenAI-agent breach of the
Australian Medicare statistics portal on the bare keyword `medicare*`, moving **G.G1 +0.40 pp**
as the night's largest application. It is banked, unpublished, and the gate is blind to it
(it measures corpus hygiene, not evidence semantics). If the gate clears before that rule is
tightened, this sheet will draw it. Noted here so the first publishing night checks for it.

## The registry did not move, and no plate needs room

`network.json` is `r10-2026-09-03` in both the staged emission and the last build: 11 axes on
each side. No new axis or sub-axis.

## State after the run

- Live: `20260919-1130`, the designed fallback; yesterday's drawing is still served.
- Repo: nothing committed by the build. This note is the only new file.
- No extractor or pull problem. The run never got that far, so exit 2 was never exercised.
