# Nightly 2026-08-18

**Exit 0.** `__BUILD=20260818-1130`, live and verified by what the sheet draws — the live page
reports nine axes `T K A C R D S P E` and registry `r6-2026-08-18`, so the r6 material reached the
browser and not merely the stamp.

Gate PASS. Pulled forecast/11 + trunk/2 · network **r6-2026-08-18** dated 2026-08-18 · climate
params extracted (intensity0 350.0, floor 40.0, decline on S, bonus C['C5','C4']) · coverage OK,
9 axes and 49 positions · entry + 11 module imports versioned.

## The registry did not move tonight

The coverage declaration was restated to `r6-2026-08-18` at 23:07 on 08-17 (`88609a9`), and the
parent emitted the same version this morning. Nothing new to letter.

The r4 hold recorded in `nightly-2026-08-17.md` is over. Between that report and this one the
drawing was re-keyed by hand through r5 (`1ed5580`) and r6 (`88609a9`), and the last published
build before tonight was already at r6 (`20260818-1038`). `HANDOFF.md` still described the hold;
it is corrected in this commit.

## What the run found: the same defect, in this project's own data

The rebuild finding from 08-17 — **a surface keyed to a position LETTER inherits whatever the
letter later means, while one keyed to a MEASURED quantity survives** — was recorded against the
parent's crisis board and claims register. It applies here too, and this time to a table this
project owns and can fix.

`RESEARCHED` and `PRIOR_R2` in `app.js` carry the evidence programme's 26 figures. The comment
above them says `// r4, 17 Aug 2026`, which is accurate: they were researched against the r4
meanings. `recommend()` looks them up by letter. `sections.js` then letters each row with
`p[1]` — **the live r6 name for that letter**. So the row draws an r4 figure under an r6 meaning,
on one line, looking exactly as checkable as the morning plate was made in August.

Twenty-two rows draw. **Thirteen attach a researched figure to a letter that now means something
else.**

| drawn on the Research tab | the figure was researched for | the r6 cell that now holds that meaning |
|---|---|---|
| `2037 TO 2050` 0.230 → 0.170 | continuous-normal, no SC in window | **T5** Method asymptote |
| `CATCH BUYS PAUSE` 0.351 → 0.290 | tractable with effort | **A4 / A5** tractable, closed / across field |
| `TRACTABLE IN CLOSED DEPLOYMENT` 0.270 → 0.250 | untested in window | **A7** Untested in window |
| `DECLARATORY ACCORD` 0.074 → 0.120 | US–CN transparency deal | no exact cell; verification is **C5** |
| `DOMAIN-CONFINED LIMIT` 0.251 → 0.270 | fragmented blocs | **R2** Contested patchwork, on the new axis |
| `DELIVERY STALLS` 0.177 → 0.170 | shock | **D4** Displacement shock |
| `RELIABILITY GATES DELIVERY` 0.559 → 0.570 | uneven by sector | partial — r6 states a mechanism where the research measured a pattern |
| `POWER AND SITING BIND` 0.255 → 0.320 | constrained: controls **and** energy | **S3 + S4** — r6 split the two, one figure covers both |
| `ACQUIESCENCE THROUGH USE` 0.259 → 0.380 ▲ | populist backlash | **P5** Backlash governs |
| `STABLE DISAPPROVAL` 0.312 → 0.280 ▼ | adoption / acquiescence | **P1** Acquiescence through use |
| `LOCAL OPPOSITION` 0.429 → 0.340 | polarized-fractured | **P4** Durable fracture |
| `VALUATION CORRECTION` 0.196 → 0.220 | deflates hard, capex-led | **E4** Capital expenditure cut |
| `CAPITAL EXPENDITURE CUT` 0.074 → 0.080 | displacement-led demand crisis | **E5** Displacement demand crisis |

Nine hold: T1, T2, T3, A1, A2, C1, S1, S2, E1. Four of the thirteen point roughly the opposite way
from what was researched, and **P1 and P2 are a straight transposition** — each figure sits on the
other's cell. The sheet currently tells a reader the evidence programme recommended raising
*acquiescence through use* from 0.259 to 0.380. It recommended raising *populist backlash*. The
live P1 prior is 0.18, so the row also shows a twenty-point gap between research and network that
does not exist.

The section heading is stale by the same mechanism: `APPLIED 13 AUG (r3) AND 17 AUG (r4)`, lettered
on a sheet drawing r6.

## The crisis board draws two wrong points

`crisis.json` comes from the parent and its question strings are 07-31 literals. The sheet takes
the **live r6 marginal** for the item's letter and letters it beside the old question:

- `no-sc-window` draws **18% NO SUPERINTELLIGENCE THIS WINDOW** reading `T.T4`, which under r6
  means arrival in **2037 to 2050**. The no-arrival cell is T5, at 0.12. The point asserts the
  opposite of the quantity it displays.
- `deal-window` draws **14% US-CHINA TRANSPARENCY DEAL BY 2032** reading `C.C3` = *Declaratory
  accord*. C carries no date, and transparency implies verification, which is C5.

Four hold: `explosive-takeoff` (T1), `alignment-fails` (A1), `hard-deflate` (E3), and
`researcher-by-2035` — the last because it is keyed on a band, a measured quantity, and a band
survives a rebuild. That is the general finding restated as a positive control.

The claims register is not lettered on this sheet, only counted, so `cl-state-laws-2026` drawing
its support from a US–CN coordination position does not reach a reader here.

## What held

The climate extractor. It was rewritten on 08-17 to read the axis, the decline map's positions and
the bonus positions out of the parent's `worldlines.py` and check them against the emitted
registry, naming no letter of its own. It came through the r6 rebuild without a change, which is
rule 10 doing exactly the work it was written for.

## Recommendation, held

Re-key `RESEARCHED` and `PRIOR_R2` **by meaning**, and bring them under `registry-covered.json` so
the coverage gate catches the next rebuild instead of the next reader. Three of the thirteen have
no clean destination — C3's transparency deal, C4's blocs (now on the R axis), S3's split into
power and trade — and for those the entry should be withdrawn and the sheet should say the research
predates the axis, rather than a figure being moved to the nearest-looking cell. That is a
judgment about the evidence programme's own output, so it is held rather than applied tonight, in
the same way a prior is.

The general form for the register: **a coverage declaration protects only what it declares.** It
was extended to catch axes, positions and meanings for the authored strings in `narrative.js`, and
it did that correctly through two rebuilds in one night. The 26 researched figures are keyed on the
same letters and were never declared, so they moved under the rebuild in silence — and the surface
that draws them letters the live meaning beside the stale figure, which makes the row look more
checkable, not less.
