# Nightly 2026-09-15 — the gate refused upstream; the sheet keeps serving 09-14

**Nothing published.** `build/nightly.sh` exited 1 at its first step: the AI Atlas corpus gate
refused, so the pull never ran, `docs/` was never stamped, and nothing was committed. The
working tree is clean at `6430118` and the live sheet still serves `20260914-1130`. This is the
designed behaviour of standing rule 2 — this project owns no forecast data, and a night the
parent cannot certify is a night this surface does not redraw. `SKIP_AUDIT` was not set; the
Atlas's own nightly owns the failure.

## The baseline that moved

    GATE: FAIL
     - events.explicit_pct moved backwards: 90.6 (baseline ≥ 91.7)

One check of nine. Everything else held or improved:

| check | 09-14 | 09-15 | |
|---|---|---|---|
| `events.explicit_pct` | 90.7 | **90.6** | ERODED — the refusal |
| `events.target_pct` | 82.6 | 82.7 | up |
| `review.pct` | 6.629 | 6.569 | up |
| `forecast.grounded_direct` | 1730 | 1736 | up |
| `events.n` | 2640 | 2664 | +24 |
| windows · cards · datekinds · eras · forecast · statelaw | 0 bad | 0 bad | clean |

`review.n` is still 175, where the baseline records 224.

The printed baseline is the stored `91.7`; the arithmetic the gate performs is
`value < baseline - 1.0`, so the line it actually failed is `90.6 < 90.7`. The gate prints the
number it stored and tests a number it derives, which is why eleven nights of "0.0000 margin"
read as comfortable. **Print the number you compared against.** The Atlas's own 09-15 note
carries the cause — an extraction window applied to the haystack — and it is Atlas code and
Atlas data, untouched from here.

## The registry did not move

`network.json` is `r10-2026-09-03`, eleven axes, in the staged emission and in the last build
alike. No plate needs room.

## What is being withheld

The parent did emit — the staged forecast is stamped 10:49 today — and the drawing behind the
gate is not the one on the sheet. Recording it now so tomorrow's publish is read as an arrival
and not a jump:

| | built (live) | staged (withheld) |
|---|---|---|
| medoid | `T3 K1 A4 C1 R4 D2 S3 P3 E3 L4 G2` | `T3 K1 A4 C1 R2 D2 S1 P2 E3 L4 G1` |
| letters that differ | — | R4→R2 · S3→S1 · P3→P2 · G2→G1 |
| medoid `p` | 9.0838e-07 | 4.2464e-07 — a 53.25% fall |
| argmax letters | `T2 K1 A2 C1 R4 D2 S1 P3 E3 L4 G4` | identical |
| agreement | 0.2954 | 0.2953 |
| crossings | 2033.25 / 2034 / 2038 / 2042.5 | **bit-identical** |
| ledger events | 23 | 15 |
| `E3` onset | 2028.5 | 2027.7 |

Four letters differ and all four crossings hold to the digit. The capability spine did not
move; a different row of the ensemble became the most central one. **A medoid's per-axis
reading is not a per-axis conclusion** — it is a selected row, so any column's change hands
centrality to a row carrying its own coordinates on axes nothing touched. Read `R2`, `S1`, `P2`
and `G1` as four coordinates of one newly-central line, never as four axes that moved.

The ledger loses eight net entries, and the composition of the loss is the interesting part:
nineteen of the live entries are absent and eleven are new, so all but four entries were
replaced. The new ones run long — `code-consolidation`, `controls-sunset`, `digital-minds`,
`late-accord`, `orbital-gigawatt`, `parity-reached`, `ai-drug-majority` — which is the r10
far-field keying doing what it was built to do on a line that reaches it.

## This surface is ready for it

Checked read-only, since the refusal costs nothing to stand down from and a second refusal
tomorrow at this project's own gates would cost a night:

- **All fifteen staged template ids are authored** in `TEMPLATE_TEXT`. Seven of them have never
  been drawn, and every one carries its text.
- **All eleven staged medoid positions are authored** in `CRITERION`, `R2`, `S1`, `P2` and `G1`
  included.

So `table_coverage.mjs` and the readout gate have nothing to refuse. The moment the parent's
corpus certifies, this sheet draws the new line without an authoring pass.

## Standing

Twelfth consecutive night that `explicit_pct` sat on its boundary, and the first that it fell
off. The margin was never 0.0000; it was the distance to a rounding boundary, and it has now
been crossed.
