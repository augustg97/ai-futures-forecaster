# Nightly 2026-09-20 — the gate refused upstream; the sheet keeps serving 09-19

**Nothing published.** `build/nightly.sh` exited 1 at its first step: the AI Atlas corpus gate
refused, so the pull never ran, `docs/` was never stamped, and nothing was committed by the
build. The working tree was clean at `a43264d` and the live sheet still serves
`20260919-1130`, verified by curl against `docs/index.html`. This is the designed behaviour of
standing rule 2 — this project owns no forecast data, and a night the parent cannot certify is
a night this surface does not redraw. `SKIP_AUDIT` was not set; the Atlas's own nightly owns
the failure and has already diagnosed it.

## The baseline that moved

    GATE: FAIL
     - review.pct moved backwards: 7.82 (baseline ≤ 6.447)

One check of nine. Everything else held or improved:

| check | 09-19 | 09-20 | |
|---|---|---|---|
| `review.pct` | 6.485 | **7.820** | ERODED — the refusal |
| `review.n` | 178 | **216** | +38, nothing removed |
| `forecast.grounded_direct` | 1826 | 1859 | +33 |
| `events.n` | 2745 | 2762 | +17 |
| `events.explicit_pct` | 90.9 | 90.9 | held |
| `events.target_pct` | 83.2 | 83.2 | held |
| `datum.max_drift` | 0.0 | 0.0 | clean |
| witness matched / disagreements | 20 / 4 | 20 / 4 | held |
| windows · cards · datekinds · eras · forecast · statelaw | 0 bad | 0 bad | clean |

**The margin, since a verdict is not a reading.** The gate prints the stored baseline `6.447`
and tests `value > baseline + 0.5`, so the number it compared against is **6.947**. At 7.820
the night failed by **0.873 pp**. The printed line understates the ceiling by the whole
tolerance — the same shape as the 09-15 refusal, where the stored `91.7` hid a test against
`90.7`. Print the number you compared against.

**The rate eroded on its numerator.** Needs-review rose 178 → 216, +21.3%; the denominator,
the event trunk, rose 2,745 → 2,762, +0.62%. A rate built to absorb a growing corpus was
carried past its ceiling by the count alone.

## The cause is a namespace the emit cannot see

Thirty-seven of the thirty-eight new needs-review lines are one line:
`dangling relationship target 'evaluations/…'`. The vault shipped a fourteenth content folder
last night — `Wiki/evaluations/`, 53 pages, six of them `git mv`'d out of `concepts/` — and the
Atlas's `Research/modeling/frames.py` enumerates the wiki's folders as a thirteen-entry literal.
Every link into the new folder reads as dangling. Strip those thirty-seven and the night stands
at **179 / 2,762 = 6.481%**, inside tolerance with 0.466 to spare, and it would have published.

This is Atlas code and Atlas data, untouched from here. The Atlas's own
`Research/nightly-reports/2026-09-20.md` carries the diagnosis, the three other places the same
cause is visible without refusing anything, and the remedy. Nothing about it is a defect of this
surface, and no queue task belongs to this project.

## The registry did not move

`network.json` is `r10-2026-09-03`, eleven axes, in the staged emission and in the last build
alike. No plate needs room.

## What is being withheld is the drawing already on the sheet

The parent did emit — the staged forecast is stamped 10:54 today — and the drawing behind the
gate is the one the sheet is serving. Every lettered quantity is identical:

| | built (live) | staged (withheld) |
|---|---|---|
| medoid | `T3 K1 A4 C1 R2 D2 S1 P2 E3 L4 G1` | identical |
| argmax | `T2 K1 A2 C1 R4 D2 S1 P3 E3 L4 G4` | identical |
| crossings | 2033.25 / 2034 / 2038 / 2042.5 | identical |
| ledger events | 15 | identical, same fifteen ids |
| onsets | `T3` 2034.0 · `K1` 2033.2 · `D2` 2033.0 · `E3` 2027.7 · `G1` 2036.0 | identical |
| agreement | 0.2953 | 0.2953 |
| medoid `p` | 4.211767e-07 | 4.211302e-07 — a 0.011% fall |
| argmax `p` | 7.277710e-06 | 7.281390e-06 — a 0.051% rise |

A sixth consecutive bit-identical drawing. Nothing is being withheld from the reader tonight but
two probabilities in their fourth significant figure. Every staged template id and every staged
medoid position is already authored, because they are the ones drawn last night; the coverage
and readout gates have nothing to refuse whenever the parent's corpus certifies.

## The delta, because "unchanged" with no number is not a reading

Marginals moved **0.205 pp L1** across the eleven axes, on three of them:

| axis | L1 | largest single position |
|---|---|---|
| A | 0.1392 pp | `A3` +0.0696 pp |
| G | 0.0436 pp | `G2` −0.0186 pp |
| D | 0.0225 pp | `D2` +0.0113 pp |

The other eight are flat to the digit. Same three-axis set as 09-19, which moved 0.274 pp. K, R
and L are flat again, as they have been every night since the 2026-09-08 measurement that named
them the axes no evidence rule writes to.

A's movement is fifteen applied `ev-safety-research` entries carrying roughly **nine distinct
developments**. OpenAI's model-misalignment framework appears three times in one night and is
now on its **fourth consecutive night**; Amodei's 12 September essay appears three times; the
Anthropic–OpenAI–Google standards-body discussions twice; Microsoft's code of conduct twice.
Google's 18 September Gemini breakout is scored as safety research a second night. The
duplication and the misclassification are both visible in the served `delta.json`, both are
Atlas defects, and the Atlas owns them. Recorded here because the only axis that moved
materially moved on them.

## Standing

Second Atlas-gate refusal in six nights, and the two have nothing in common but the exit code.
09-15 was a measurement boundary crossed after twelve nights sitting on it. Tonight is a
producer's namespace list stored as a literal in the consumer — the emit reporting a total while
unable to census what it is missing. The sheet's behaviour was correct in both: it drew nothing
and kept serving the last certified drawing.
